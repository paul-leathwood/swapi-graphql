# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build graphql-yoga`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t graphql-yoga`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system graphql-yoga && \
          adduser --system -G graphql-yoga graphql-yoga

COPY dist/apps/graphql-yoga graphql-yoga
RUN chown -R graphql-yoga:graphql-yoga .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix graphql-yoga --omit=dev -f install

CMD [ "node", "graphql-yoga" ]
